{% if student_list %}
  <table style="width:100%">
    <tr><th>Name</th> <th colspan="2">Telephone</th> </tr>
    {% for student in student_list %}
      <tr><td>{{ student.name }}({{ student.rollno }})</td></tr>
    {% endfor %}
  <tr>
  </tr>
</table> 
{% else %}
    <p>No students are available.</p>
{% endif %}

